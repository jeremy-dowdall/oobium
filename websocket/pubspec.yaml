name: oobium_websocket
description: Websocket implementation for clients and servers
version: 0.1.0+1
repository: https://github.com/jeremy-dowdall/oobium

publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  collection: ^1.15.0
  xstring: ^1.2.0

dev_dependencies:
  test: ^1.15.3
  build_runner: ^2.0.4
